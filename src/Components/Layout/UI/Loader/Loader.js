import React from 'react';
import classes from './Loader.module.css';
// this is a css spiner from some web site
const Loader = (props) => (
    <div className={classes.loader}>Loading...</div>
)

export default Loader;